create view Places as
  extract regex /China|French|Xinjiang/
    on D.text
    as Name
  from Document D;

create view Do as
  extract pattern (<P.Name>) /[a-zA-Z]+s/
  return group 0 as Does
    and group 1 as Place
  from Places P;

output view Places;
output view Do;